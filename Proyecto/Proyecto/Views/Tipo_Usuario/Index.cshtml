@model IEnumerable<Proyecto.Tipo_Usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Tipo de Usuario</h2>

<p>
    @Html.ActionLink("Crear Tipo de Usuario", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label("Tipo de Usuario")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Usuario1)
        </td>
        <td>
            @Html.ActionLink("Editar Tipo de Usuario", "Edit", new { id=item.id_tipo }) |
            @Html.ActionLink("Detalle Tipo de Usuario", "Details", new { id=item.id_tipo }) |
            @Html.ActionLink("Eliminar Tipo de Usuario", "Delete", new { id=item.id_tipo })
        </td>
    </tr>
}

</table>
