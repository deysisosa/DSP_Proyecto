@model Proyecto.Tipo_Usuario

@{
    ViewBag.Title = "Delete";
}

<h2>Eliminar Tipo de Usuario</h2>

<h3>Estás seguro que quieres eliminar esto?</h3>
<div>
    <h4>Tipo de Usuario</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.Label("Tipo de Usuario")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Tipo_Usuario1)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar Tipo de Usuario" class="btn btn-default" /> |
            @Html.ActionLink("Volver", "Index")
        </div>
    }
</div>
